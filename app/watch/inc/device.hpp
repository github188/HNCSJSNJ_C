#ifndef _DEVICE_HPP
#define _DEVICE_HPP

// the base class for all sort of devices
// it can supply the virtual functions.
class Device {
public:
  Device();
  virtual bool open();
  virtual bool close();
virtual 

#endif //_DEVICE_HPP
